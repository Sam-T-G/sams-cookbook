{
  "name": "claude-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Edge relay that holds the Anthropic API key server side and verifies Apple App Attest, so an iOS app never ships the key.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
